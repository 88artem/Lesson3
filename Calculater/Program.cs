using Calculater;
using System.Runtime.InteropServices.Marshalling;
UICalculate _uiCalculate = new();
while (true)
{
    Console.WriteLine("Введите операцию которую хотите совершить, а затем введите значения!");
    try
    {
        _uiCalculate.Nums(float.Parse(Console.ReadLine(), Console.ReadLine();
    }
    catch (Exception ex)
    {
        Console.WriteLine($"{ex.Message}");
    }
    try
    {
        _uiCalculate.Nums(float.Parse(Console.ReadLine(), float.Parse(Console.ReadLine(), Console.ReadLine();
    }
    catch (Exception ex)
    {
        Console.WriteLine($"{ex.Message}");
    }


}
